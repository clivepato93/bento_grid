module.exports = {
    //.... All the initial code
    theme: {
        extend: {
            fontFamily: {
                'DMSans' : ['DMSans', 'sans-serif'],  // after you font, add some fonts separated by commas to handle fallback.
            }
        }
    }
    //.... All the initial code
}